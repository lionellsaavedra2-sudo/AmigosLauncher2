<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="#121212"
    android:padding="24dp">

    <!-- Animación / Cargador -->
    <ProgressBar
        android:id="@+id/pb_loading"
        style="?android:attr/progressBarStyle"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:indeterminateTint="#00E676"
        android:layout_marginBottom="24dp" />

    <!-- Título de Instancia -->
    <TextView
        android:id="@+id/tv_instance_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Preparando Instancia..."
        android:textColor="#FFFFFF"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_marginBottom="12dp"
        android:gravity="center"/>

    <!-- Detalle / Estado de Descarga -->
    <TextView
        android:id="@+id/tv_sync_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Consultando servidores..."
        android:textColor="#888888"
        android:textSize="14sp"
        android:layout_marginBottom="24dp"
        android:gravity="center"/>

    <!-- Barra de progreso horizontal -->
    <ProgressBar
        android:id="@+id/pb_sync_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="8dp"
        android:progressTint="#00E676"
        android:progressBackgroundTint="#333333"
        android:max="100"
        android:progress="0"
        android:layout_marginHorizontal="16dp"
        android:layout_marginBottom="12dp"/>

    <!-- Detalle numérico (ej. 2/5) -->
    <TextView
        android:id="@+id/tv_progress_detail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0/0 archivos"
        android:textColor="#555555"
        android:textSize="12sp"
        android:gravity="center"/>
</LinearLayout>
